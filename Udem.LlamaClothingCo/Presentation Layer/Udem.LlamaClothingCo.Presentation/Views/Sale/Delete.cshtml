@model Udem.LlamaClothingCo.Entities.Sale

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Sale</legend>

    <div class="display-label">Client</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Client.FirstName)
    </div>

    <div class="display-label">SaleTotal</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.SaleTotal)
    </div>

    <div class="display-label">Date</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Date)
    </div>

    <div class="display-label">ShippingAddresses</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ShippingAddresses.Street)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
